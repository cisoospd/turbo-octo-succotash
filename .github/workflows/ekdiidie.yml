on: workflow_dispatch
jobs:
  zhalost:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: |
          RUSTFLAGS="-C target-cpu=native" cargo build --release
          cd target/release
          ./my_app 31.76.110.140 443 300
